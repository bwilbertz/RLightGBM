 ## This assumes that the LGBM_HOME variable points to the LightGBM dir
 PKG_CPPFLAGS=-I$(LGBM_HOME)/include -I../inst/include
 PKG_LIBS=-L$(LGBM_HOME) -llightgbm $(shell "${R_HOME}/bin${R_ARCH_BIN}/Rscript" -e "Rcpp:::LdFlags()")